# 2026-06-14 MVP 构建验证

## 范围

- 工程骨架创建。
- 姿态评分、提醒决策、本地存储。
- 守护、统计、设置三页 ArkUI。
- 真实相机检测：CameraKit 拍照抽帧、JPEG 解码、Core Vision 骨架与人脸检测。
- 算法升级：D1-D5 正面摄像头代理指标、10 帧中位数基线校准、窗口常亮低亮守护。

## 结果

- `./scripts/verify-app.sh` 通过，内部执行 `./scripts/build-hap.sh --stacktrace`。
- 产物：`entry/build/default/outputs/default/entry-default-signed.hap`。
- 签名：已绑定 `default` 签名配置，`SignHap` 通过。
- `./scripts/install-run.sh` 可用于真机安装并启动 `com.longxin.zuozhile/EntryAbility`。
- `aa dump -r` 显示 `com.longxin.zuozhile` 进程为 `FOREGROUND`。
- `com.longxin.zuozhile.keepawake` LaunchAgent 已配置 480 秒触发一次，保活脚本退出码为 0。
- `./scripts/start-phone-keepawake.sh` 已验证可重载 LaunchAgent。
- `./scripts/keepawake-status.sh` 已验证可读取最近点击日志。
- DevEco Studio 已打开 `/Users/zhanglongxin/Desktop/longxincode/zuozhile` 工程。
- 12:15:07 与 12:23:08 之间由 LaunchAgent 自动完成 8 分钟保活点击，退出码为 0。
- 新增 `CoreVisionPostureDetector.ets` 后 `./scripts/verify-app.sh` 通过。
- 新增开始守护前 CAMERA 动态权限请求后 `./scripts/install-run.sh` 通过，设备进程为 `FOREGROUND`。
- 新增 `CameraProbeService.ets` 并接入开始守护路径后 `./scripts/install-run.sh` 通过，设备进程为 `FOREGROUND`。
- 保活 LaunchAgent 在 12:31:08 与 12:39:08 连续完成 8 分钟自动点击，退出码为 0。
- `Index.ets` 当前仍承载守护、统计和设置三页，后续功能稳定后可继续拆分页面 Builder。
- 12:34 版本 signed HAP 安装启动通过，`com.longxin.zuozhile` 设备进程为 `FOREGROUND`。
- 14:25 版本 signed HAP 安装启动通过，设备 `5ZGYD25A19000137` 上
  `com.longxin.zuozhile` 进程为 `FOREGROUND`，pid `51873`。
- 真实相机检测已在守护页启动，UI 显示“结束守护”，并按 2 秒周期持续采样。
- 修复 JPEG 尺寸来源：不再用 `photo.main.size` 推导解码尺寸，改为
  `ImageSource.getImageInfoSync()` 读取源图真实尺寸。
- `hilog -P 51873 -T ZuozhileCamera` 验证真实帧解码为
  `source=480x480 target=480x480`，`PixelMap actual=480x480 format=3 stride=1920`。
- Core Vision 日志连续显示 `骨架与人脸检测完成`，未再出现 `720x1` PixelMap 导致的参数校验失败。
- 统计页已出现 14:27 附近连续采样记录，最近时段 14:00 桶和最近记录均有数据。
- 新增 `PostureCalibration.ets`，`记录基线` 改为 10 帧采样并对各基线字段取中位数。
- `PostureEvaluator.ets` 已按新算法文档落地 D1-D5：
  低头/颈前屈、身体下滑、含胸/驼背、肩部歪斜、嘴巴张合。
- 新增 `GuardWindowService.ets`，开始守护时 `setWindowKeepScreenOn(true)` 并设置低窗口亮度，停止守护时恢复。
- 15:00 版本 `./scripts/verify-app.sh` 通过，`./scripts/install-run.sh` 安装启动通过，设备进程 pid `7707`。
- 真机 UI 验证：
  - 开始守护后显示 `ON`、`前台守护中`、`结束守护`。
  - 状态区显示 `守护窗口常亮 · 低亮度` 和真实相机检测。
  - 停止后显示 `OFF`、`开始守护`、`守护窗口已恢复`、`真实相机已释放`。
- `hilog -P 7707 -T ZuozhileGuard` 验证窗口守护开启/恢复日志。
- `hilog -P 7707 -T ZuozhileCamera` 验证真实相机持续生成 `actual=480x480` PixelMap，Core Vision 完成骨架与人脸检测。
- 保活 LaunchAgent 当前仍为 480 秒周期，最近自动点击记录到 14:55:16，退出码为 0。
- 新增 D1-D5 分项提醒类型：
  - `PostureResult` 增加 `activeAlerts`、`primaryAlert`。
  - `SessionRecord` 增加 `alertType` 并兼容旧记录。
  - `ReminderEngine` 改为 D1-D5 独立去抖、120 秒冷却、D2 > D1 > D3 > D4 > D5 优先级。
- 15:14 版本 `./scripts/verify-app.sh` 通过，`./scripts/install-run.sh` 安装启动通过，设备进程 pid `14883`。
- 真机验证：
  - 守护页启动后显示 `ON`、`前台守护中`、`守护窗口常亮 · 低亮度`。
  - 真实相机日志持续显示 `actual=480x480` 和 `骨架与人脸检测完成`。
  - 无持续异常时显示 `无提醒`，统计页最近记录正常渲染。
  - 停止守护后显示 `守护窗口已恢复`、`真实相机已释放`。
  - 最近自动保活点击记录到 15:11:17，退出码为 0。
- 新增前台提醒 UI：
  - 轻提醒显示为守护页内紧凑横幅。
  - 强提醒显示为前台遮罩，支持点“知道了”关闭，并在 8 秒后自动收起。
  - 停止守护、清空数据或重新开始守护时会清理提醒状态和强提醒定时器。
- 15:30 版本 `./scripts/verify-app.sh` 通过，`./scripts/install-run.sh` 安装启动通过，设备进程 pid `22772`。
- 真机验证：
  - 首页三项卡片保持紧凑横排，`开始守护` 坐标为 `[58,1392][1262,1561]`，首屏完整可见。
  - 开始守护后显示 `ON`、`前台守护中`、`结束守护`。
  - `ZuozhileGuard` 日志显示 `守护窗口常亮 · 低亮度`，停止后显示 `守护窗口已恢复`。
  - `ZuozhileCamera` 日志持续显示 `source=480x480 target=480x480`、`actual=480x480` 和 `骨架与人脸检测完成`。
  - 停止后显示 `OFF`、`开始守护`、`守护窗口已恢复`、`真实相机已释放`。
- 17:25 版本 `./scripts/verify-app.sh` 通过，`./scripts/install-run.sh` 安装启动通过，设备进程 pid `28882`。
- 灵敏度设置真机验证：
  - 设置页仅显示 `标准` 和 `灵敏` 两档，不再显示 `稳健`。
  - 点击 `灵敏` 后显示 `当前：灵敏 · 2 秒`，右侧为 `已选`；点击 `标准` 后显示 `当前：标准 · 2 秒`，左侧为 `已选`。
  - 切换灵敏度会重置提醒状态，避免旧档位连续触发状态延续。
- D3 含胸/肩前扣容错：
  - 标准档放宽对肩宽压缩单一信号的判定，轻微配合脸部靠近时优先显示 `身体靠前`，明显异常才显示 `肩膀前扣`。
  - 前台提醒文案改为“身体靠前/坐回椅背”，减少把正常键盘鼠标操作直接归因到肩膀前扣。
- 首页显示稳定：
  - 三项指标增加两帧稳定策略，异常状态需要连续出现两帧才更新到首页，恢复正常立即更新。
  - 开始/停止守护、切换灵敏度、切换检测源和清空数据时会重置显示候选状态。
- 主流程回归：
  - 守护页启动后显示 `ON`、`前台守护中`、`结束守护`、`坐姿在线`、`无提醒`。
  - 停止后显示 `OFF`、`开始守护`。

## 说明

当前版本默认仍可使用模拟采样；设置页开启“真实相机检测”后，会使用前置相机拍照抽帧并在本机内存中完成 Core Vision 检测。图片、PixelMap 和关键点原始坐标不落盘。
