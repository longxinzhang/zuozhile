# 2026-06-16 沉浸光感 UI 修正验证

## 范围

- 按《坐直了_HarmonyOS沉浸光感_UI修正文档》修正首页状态显示、主导航结构和卡片密度。
- 调试功能从正式底部导航移到设置页底部的 `开发者调试` 入口。
- 本次只改 UI 显示、布局和文案映射，不修改识别算法、阈值、提醒规则、采样链路或存储结构。

## 结果

- `./scripts/verify-app.sh` 通过。
- signed HAP 真机安装启动成功，设备进程 pid `59506`。
- idle 状态显示 `前台守护未开启`、`等待开始守护`、评分 `--`、三项 `待识别`，没有出现 `坐姿在线`、实时评分或本次报告。
- finished 状态显示 `本次守护已结束`、`重新开始守护` 和 `本次守护报告`。
- guarding 状态点击启动后显示 `结束守护`，权限状态更新，停止后恢复到 finished 报告态。
- 底部导航只显示 `守护 / 统计 / 指南 / 设置`，不再显示 `调试`。
- 设置页底部显示 `开发者调试` 入口，可展开检测日志、精度校准和参数视图。

## 注意

- 参考图中的复杂材质、设备外框和外部宣发背景不属于 App 内实际界面，本次聚焦 App 内 UI 状态、密度和沉浸光感层级。
- 为避免影响真实相机链路，隐藏 `XComponent` 仍保留，只移出可视区域。
