# 2026-06-15 守护报告验证

## 范围

- 用户点击 `结束守护` 后生成本次守护简短报告。
- 报告展示时长、均分、提醒次数、简短结论和一条建议。
- 内部停止守护流程不生成报告。

## 结果

- `./scripts/verify-app.sh` 通过。
- `./scripts/install-run.sh` 安装并启动 signed HAP 成功，设备进程 pid `52127`。
- 真机点击 `开始守护`，等待真实相机采样后点击 `结束守护`。
- 首页恢复 `OFF`、`开始守护`，并显示 `本次守护报告`。
- 本次短守护验证显示 `少于1分钟 · 69分 · 提醒0次`、`本次均分偏低，但没有形成稳定单项问题。` 和一条建议。

## 说明

- 这次只新增报告展示和汇总逻辑，不调整识别算法、阈值、灵敏度和提醒规则。
