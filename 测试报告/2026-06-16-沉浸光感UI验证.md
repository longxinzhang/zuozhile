# 2026-06-16 沉浸光感 UI 验证

## 范围

- 根据《坐直了_HarmonyOS沉浸光感优化开发文档》进行第一版 UI 优化。
- 覆盖首页、底部导航、统计页、指南页、设置页、调试页和弹窗的视觉样式。
- 本次只调整颜色、圆角、阴影、卡片层级、按钮样式和隐藏相机占位方式，不修改识别算法、阈值、提醒规则、采样链路或本地存储结构。

## 结果

- `./scripts/verify-app.sh` 通过。
- signed HAP 真机安装启动成功，设备进程 pid `11996`。
- 首页首屏可见：标题、前台守护状态、主评分卡、三项核心指标、`开始守护` 和识别建议卡片。
- 底部导航保留 `守护 / 统计 / 指南 / 调试 / 设置` 五个入口，并改为悬浮胶囊视觉。
- 点击 `开始守护` 后真机进入 `前台守护中`，显示 `结束守护`，采样时间和评分更新；停止后回到 `前台守护待开启` 和 `开始守护`，并显示本次守护报告。
- 隐藏相机 `XComponent` 已移出标题栏，截图确认不会在 UI 上露出黑点。

## 注意

- 文档中的四 Tab 示例为旧结构；本版本保留已经实现的 `调试` tab。
- 本次没有新增图标资源，也没有拆分页面文件，优先在现有 ArkUI 结构中完成视觉升级，降低功能回归风险。
