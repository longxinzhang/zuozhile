# 2026-06-16 检测调试 Tab 验证

## 范围

- 新增底部 `调试` tab，并保持原有 `守护`、`统计`、`指南`、`设置` 页面可达。
- 调试页包含 `检测日志`、`精度校准`、`参数` 三个子页签。
- 本版本只读取现有守护记录、最近采样、校准基线和设置参数，不新增截图落盘、数据库表或自适应阈值写回。

## 结果

- `./scripts/verify-app.sh` 通过。
- signed HAP 真机安装启动成功，设备进程 pid `59045`。
- 真机布局导出确认底部 TabBar 显示 `守护 / 统计 / 指南 / 调试 / 设置`。
- `检测日志` 页显示今日提醒统计、最近帧关键点/姿态摘要、最近记录和 `正确 / 有误` 标注按钮。
- `精度校准` 页显示 7 步主动校准计划，并提供 `先记录健康基线` 入口。
- `参数` 页显示当前档位、抽帧间隔、基线状态，以及 `PITCH_HIGH` 等主要阈值。

## 注意

- `正确 / 有误` 反馈当前只更新本页可见提示，不持久化。
- 规格书中的快照截图、骨骼叠加图、relationalStore 表和自适应阈值引擎尚未接入。
- 本次没有修改识别算法阈值、提醒优先级或相机采样链路。
